#include "main.h"
#include <stdio.h>

/**
 * main - prints its name
 * @argc: number of lines in argv argument
 * @argv: argument vector
 * Return: 0 when successful
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
