#include "main.h"

/**
 * mul - multiplies two integers
 * @x: The first number to multiply
 * @y: The seconf number to multiply to first number
 * Return: The result of multiplication between x param and y param
 */

int mul(int x, int y)

{
	return (x * y);
}
